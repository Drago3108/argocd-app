apiVersion: v1
kind: Service
metadata:
  labels:
    app: dhamu-app
  name: dhamu-app-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: dhamu-app
  type: NodePort
